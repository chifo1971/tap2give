pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        // Allow Stripe Terminal SDK dependencies
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "Tap2Give"
include ':app'